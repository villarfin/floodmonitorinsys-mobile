import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  badge: {
    borderWidth: 1,
    borderRadius: 999,
    paddingHorizontal: 10,
    paddingVertical: 4,
  },
  label: {
    fontWeight: "700",
    fontSize: 12,
  },
});

